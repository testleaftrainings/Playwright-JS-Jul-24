/**
 * Type inference - infering the data type
 * 1. Implicit type inference
 * When the compiler is able to infer the data type on its own from
 * the value that user is assigning
 * 2. Explicit type inference
 * When the user explicitly mention the data type
 * 
 */

//Implicit Type inference
let password = "crmsfa";
let hadLunch = true;

//Explicit type inference
let statusCode:string = "0030120086792008";
console.log(statusCode);


/**
 * 
 * const browser: Browser = await chromium.launch();
 * 
 * varDec varName: PageInterface (Return type)
 * const page: Page = await browser.newPage();
 * Locators
 */

